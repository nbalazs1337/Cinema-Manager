package percentage;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class Percentage {
    @Test
    void testToString() {

        Percentage percentage = new Percentage();
        assertEquals("",percentage.toString() );

    }
}
